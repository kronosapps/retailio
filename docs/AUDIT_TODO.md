# Audit — follow-ups (parked)

Ops audit is live. Optional next:

- [ ] Export CSV from Audit log
- [ ] Per-user filter chips (staff picker)
- [ ] Sheets sync tab for `ops_audit` (if EOD pack needs it)
- [ ] Immutable retention policy / archive older than N days to cold storage
- [ ] Staff deactivate / role-change audit when those APIs land

Do not merge into `crm_audit` — keep customer vs store security trails separate.
